{
  "id": "data-stream",
  "title": "Data Stream",
  "description": "Flowing data particles with real-time chart visualization and metric bars.",
  "version": "1.0.0",
  "mode": "template",
  "duration": 6,
  "fps": 30,
  "resolution": {
    "width": 1920,
    "height": 1080
  },
  "backgroundColor": "#030712",
  "tags": [
    "data-viz",
    "2d",
    "particles"
  ],
  "template": {
    "layers": [
      {
        "id": "main",
        "name": "Data Stream",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 0,
        "endTime": 6,
        "visible": true,
        "opacity": 1,
        "dependencies": [
          "canvas2d",
          "gsap",
          "d3"
        ],
        "properties": {
          "dependencies": [
            "canvas2d",
            "gsap",
            "d3"
          ],
          "code": "\n  function clamp(v) { return Math.max(0, Math.min(1, v)); }\n  function easeOut(v) { v = clamp(v); return 1 - Math.pow(1 - v, 3); }\n  function easeInOut(v) { v = clamp(v); return v < 0.5 ? 4*v*v*v : 1 - Math.pow(-2*v+2,3)/2; }\n  function easeElastic(v) { v = clamp(v); return v === 0 || v === 1 ? v : Math.pow(2,-10*v)*Math.sin((v*10-0.75)*((2*Math.PI)/3))+1; }\n  function lerp(a,b,t) { return a + (b-a)*t; }\n  function roundedRect(ctx,x,y,w,h,r) {\n    ctx.beginPath();\n    ctx.moveTo(x+r,y); ctx.lineTo(x+w-r,y);\n    ctx.quadraticCurveTo(x+w,y,x+w,y+r);\n    ctx.lineTo(x+w,y+h-r);\n    ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);\n    ctx.lineTo(x+r,y+h);\n    ctx.quadraticCurveTo(x,y+h,x,y+h-r);\n    ctx.lineTo(x,y+r);\n    ctx.quadraticCurveTo(x,y,x+r,y);\n    ctx.closePath();\n  }\n  function drawBg(ctx,w,h,t) {\n    const g = ctx.createLinearGradient(0,0,w,h);\n    g.addColorStop(0,'#030712');\n    g.addColorStop(0.5,'#07172A');\n    g.addColorStop(1,'#15102A');\n    ctx.fillStyle = g; ctx.fillRect(0,0,w,h);\n    ctx.save(); ctx.globalCompositeOperation='screen'; ctx.globalAlpha=0.18;\n    const c1=ctx.createRadialGradient(w*0.2,h*0.15,10,w*0.2,h*0.15,500);\n    c1.addColorStop(0,'#00D4FF'); c1.addColorStop(1,'rgba(0,212,255,0)');\n    ctx.fillStyle=c1; ctx.fillRect(0,0,w,h);\n    const c2=ctx.createRadialGradient(w*0.85,h*0.75,10,w*0.85,h*0.75,550);\n    c2.addColorStop(0,'#B487FF'); c2.addColorStop(1,'rgba(180,135,255,0)');\n    ctx.fillStyle=c2; ctx.fillRect(0,0,w,h);\n    ctx.restore();\n  }\n  function drawGrid(ctx,w,h,t,alpha) {\n    ctx.save(); ctx.globalAlpha=0.07*alpha; ctx.strokeStyle='#DFF6FF';\n    for(let x=-200;x<w+200;x+=80){ ctx.beginPath(); ctx.moveTo(x+(t*14)%80,h*0.1); ctx.lineTo(x-300+(t*14)%80,h); ctx.stroke(); }\n    ctx.restore();\n  }\n\nfunction createRenderer() {\n  const data=[0.65,0.82,0.54,0.91,0.73,0.88,0.61,0.95];\n  const colors=['#00D4FF','#7BD88F','#F2AA4C','#B487FF'];\n  function render(t,ctx2) {\n    const ctx=ctx2.mainContext,w=ctx2.width,h=ctx2.height;\n    drawBg(ctx,w,h,t); drawGrid(ctx,w,h,t,easeOut(t/1));\n    ctx.save(); ctx.globalCompositeOperation='screen';\n    for(let i=0;i<60;i++){\n      const speed=80+i*3, x=(i*137+t*speed)%(w+100)-50;\n      const y=h*0.2+Math.sin(t*1.5+i*0.5)*h*0.25+(i%7)*h*0.08;\n      ctx.globalAlpha=0.4+0.3*Math.sin(t*2+i);\n      ctx.fillStyle=colors[i%4]; ctx.beginPath(); ctx.arc(x,y,2+(i%3),0,Math.PI*2); ctx.fill();\n    }\n    ctx.restore();\n    const panel=easeOut((t-0.8)/1);\n    ctx.save(); ctx.globalAlpha=panel;\n    roundedRect(ctx,w*0.1,h*0.55,w*0.8,h*0.32,32);\n    ctx.fillStyle='rgba(5,12,24,0.8)'; ctx.fill();\n    ctx.strokeStyle='rgba(255,255,255,0.15)'; ctx.stroke();\n    const bw=(w*0.8-80)/data.length;\n    for(let i=0;i<data.length;i++){\n      const v=data[i]*(0.7+0.3*Math.sin(t*1.8+i));\n      const bh=v*h*0.22, bx=w*0.1+40+i*bw, by=h*0.82-bh;\n      ctx.fillStyle=colors[i%4]; roundedRect(ctx,bx,by,bw-12,bh,8); ctx.fill();\n    }\n    ctx.font='900 36px Arial,sans-serif'; ctx.fillStyle='#F8FBFF';\n    ctx.fillText('Data Stream',w*0.14,h*0.62);\n    ctx.font='600 22px Arial,sans-serif'; ctx.fillStyle='#9CB7CC';\n    ctx.fillText('Real-time metrics visualization',w*0.14,h*0.67);\n    ctx.restore();\n  }\n  return {render};\n}"
        }
      }
    ]
  }
}