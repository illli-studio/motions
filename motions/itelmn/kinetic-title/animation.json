{
  "id": "kinetic-title",
  "title": "Kinetic Title",
  "description": "Bold kinetic typography with staggered letter reveal and gradient accents.",
  "version": "1.0.0",
  "mode": "template",
  "duration": 4,
  "fps": 30,
  "resolution": {
    "width": 1920,
    "height": 1080
  },
  "backgroundColor": "#030712",
  "tags": [
    "text",
    "2d",
    "transition"
  ],
  "template": {
    "layers": [
      {
        "id": "main",
        "name": "Kinetic Title",
        "type": "custom-code",
        "zIndex": 1,
        "startTime": 0,
        "endTime": 4,
        "visible": true,
        "opacity": 1,
        "dependencies": [
          "canvas2d",
          "gsap",
          "d3"
        ],
        "properties": {
          "dependencies": [
            "canvas2d",
            "gsap",
            "d3"
          ],
          "code": "\n  function clamp(v) { return Math.max(0, Math.min(1, v)); }\n  function easeOut(v) { v = clamp(v); return 1 - Math.pow(1 - v, 3); }\n  function easeInOut(v) { v = clamp(v); return v < 0.5 ? 4*v*v*v : 1 - Math.pow(-2*v+2,3)/2; }\n  function easeElastic(v) { v = clamp(v); return v === 0 || v === 1 ? v : Math.pow(2,-10*v)*Math.sin((v*10-0.75)*((2*Math.PI)/3))+1; }\n  function lerp(a,b,t) { return a + (b-a)*t; }\n  function roundedRect(ctx,x,y,w,h,r) {\n    ctx.beginPath();\n    ctx.moveTo(x+r,y); ctx.lineTo(x+w-r,y);\n    ctx.quadraticCurveTo(x+w,y,x+w,y+r);\n    ctx.lineTo(x+w,y+h-r);\n    ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);\n    ctx.lineTo(x+r,y+h);\n    ctx.quadraticCurveTo(x,y+h,x,y+h-r);\n    ctx.lineTo(x,y+r);\n    ctx.quadraticCurveTo(x,y,x+r,y);\n    ctx.closePath();\n  }\n  function drawBg(ctx,w,h,t) {\n    const g = ctx.createLinearGradient(0,0,w,h);\n    g.addColorStop(0,'#030712');\n    g.addColorStop(0.5,'#07172A');\n    g.addColorStop(1,'#15102A');\n    ctx.fillStyle = g; ctx.fillRect(0,0,w,h);\n    ctx.save(); ctx.globalCompositeOperation='screen'; ctx.globalAlpha=0.18;\n    const c1=ctx.createRadialGradient(w*0.2,h*0.15,10,w*0.2,h*0.15,500);\n    c1.addColorStop(0,'#00D4FF'); c1.addColorStop(1,'rgba(0,212,255,0)');\n    ctx.fillStyle=c1; ctx.fillRect(0,0,w,h);\n    const c2=ctx.createRadialGradient(w*0.85,h*0.75,10,w*0.85,h*0.75,550);\n    c2.addColorStop(0,'#B487FF'); c2.addColorStop(1,'rgba(180,135,255,0)');\n    ctx.fillStyle=c2; ctx.fillRect(0,0,w,h);\n    ctx.restore();\n  }\n  function drawGrid(ctx,w,h,t,alpha) {\n    ctx.save(); ctx.globalAlpha=0.07*alpha; ctx.strokeStyle='#DFF6FF';\n    for(let x=-200;x<w+200;x+=80){ ctx.beginPath(); ctx.moveTo(x+(t*14)%80,h*0.1); ctx.lineTo(x-300+(t*14)%80,h); ctx.stroke(); }\n    ctx.restore();\n  }\n\nfunction createRenderer() {\n  const lines=['MOTION','GRAPHICS'];\n  function render(t,ctx2) {\n    const ctx=ctx2.mainContext,w=ctx2.width,h=ctx2.height;\n    drawBg(ctx,w,h,t); drawGrid(ctx,w,h,t,1);\n    const colors=['#00D4FF','#7BD88F','#F2AA4C','#B487FF'];\n    for(let li=0;li<lines.length;li++){\n      const text=lines[li], fs=li===0?140:120;\n      ctx.font='900 '+fs+'px Arial,sans-serif';\n      const tw=ctx.measureText(text).width, startX=(w-tw)/2, baseY=h*0.38+li*fs*1.1;\n      for(let i=0;i<text.length;i++){\n        const delay=li*0.15+i*0.08, p=easeElastic(clamp((t-delay)/0.6));\n        const x=startX+ctx.measureText(text.slice(0,i)).width;\n        ctx.save(); ctx.globalAlpha=p;\n        ctx.translate(x+fs*0.35, baseY+(1-p)*80);\n        ctx.rotate((1-p)*0.3*(i%2?1:-1));\n        const g=ctx.createLinearGradient(x,baseY-fs,x,baseY);\n        g.addColorStop(0,colors[i%colors.length]); g.addColorStop(1,'#F8FBFF');\n        ctx.fillStyle=g; ctx.fillText(text[i],0,0);\n        ctx.restore();\n      }\n    }\n    const bar=easeOut((t-2)/0.8);\n    ctx.save(); ctx.globalAlpha=bar;\n    roundedRect(ctx,w/2-200,h*0.72,400,6,3);\n    const bg=ctx.createLinearGradient(w/2-200,0,w/2+200,0);\n    bg.addColorStop(0,'#00D4FF'); bg.addColorStop(0.5,'#7BD88F'); bg.addColorStop(1,'#F2AA4C');\n    ctx.fillStyle=bg; ctx.fill();\n    ctx.restore();\n  }\n  return {render};\n}"
        }
      }
    ]
  }
}