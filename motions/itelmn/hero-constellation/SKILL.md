# Hero Constellation

Nodes appear, links connect, then the graph gently rotates. Function: **network / graph / system map** (textless).

## Specs

- Fixed deterministic point set (no Math.random)
- Core node pulses after links appear
- Entry: `index.html`
